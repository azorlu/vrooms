@using Vrooms.Domain.Entities
@model IEnumerable<Book>

@{
    ViewBag.Title = "Books";
}

@foreach (var p in @Model)
{
    <div>
        <h3>@p.Title</h3>
        @p.Description
        <h4>@p.Author</h4>
    </div>
}
